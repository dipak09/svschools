@extends('layouts.app')
@section('title', 'Edit '.ucfirst($type))
@section('content')
<section class="page-head"><div class="container"><a href="{{ route('admin.classes') }}">Classes &amp; Sections</a><h1>Edit {{ ucfirst($type) }}</h1></div></section>
<section class="section"><div class="container"><div class="form-card"><form method="POST" action="{{ $type === 'class' ? route('admin.classes.update',$record) : route('admin.sections.update',$record) }}">@csrf @method('PUT')<div class="mb-3"><label class="form-label" for="structure_name">{{ $type === 'class' ? 'Class Name' : 'Section Name' }}</label><input class="form-control" id="structure_name" name="name" value="{{ $record->name }}" required></div>@if($type === 'class')<div class="mb-3"><label class="form-label" for="structure_year">Academic Year</label><select class="form-select" id="structure_year" name="academic_year_id" required>@foreach($years as $year)<option value="{{ $year->id }}" @selected($record->academic_year_id === $year->id)>{{ $year->name }}</option>@endforeach</select></div><div class="mb-3"><label class="form-label" for="structure_teacher">Class Teacher</label><select class="form-select" id="structure_teacher" name="class_teacher_id"><option value="">No class teacher</option>@foreach($teachers as $teacher)<option value="{{ $teacher->id }}" @selected($record->class_teacher_id === $teacher->id)>{{ $teacher->name }}</option>@endforeach</select></div>@else<div class="mb-3"><label class="form-label" for="structure_class">Class</label><select class="form-select" id="structure_class" name="school_class_id" required>@foreach($classes as $class)<option value="{{ $class->id }}" @selected($record->school_class_id === $class->id)>{{ $class->name }}</option>@endforeach</select></div>@endif<button class="btn btn-sv">Save Changes</button></form></div></div></section>
@endsection
